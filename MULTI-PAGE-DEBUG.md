# Multi-Page Debug Visualization

## 🎨 **Оновлена логіка фарбування для всіх сторінок**

### 📋 **Кольори блоків:**

#### **Перша сторінка (Direct Pass):**
- 🟢 **Зелений** (`rgba(0, 255, 0, 0.3)`) - блок буде перенесений
- 🔴 **Червоний** (`rgba(255, 0, 0, 0.3)`) - блок залишається

#### **Інші сторінки (Boundary Pass):**
- 🔵 **Синій** (`rgba(0, 150, 255, 0.3)`) - блок буде перенесений з межі
- 🟠 **Помаранчевий** (`rgba(255, 150, 0, 0.3)`) - блок залишається на межі

### 📏 **Кольори меж:**

- 🔵 **Синій** (`rgba(0, 0, 255, 0.3)`) - межі сторінок (top/bottom)
- 🟢 **Зелений** (`rgba(0, 255, 0, 0.5)`) - межа безпечної зони (60px від низу)
- 🟡 **Жовтий** (`rgba(255, 255, 0, 0.7)`) - межа для boundary analysis

### 🔄 **Двохетапна логіка:**

#### **Етап 1: Direct Pass (Перша сторінка)**
```javascript
for (let pass = 0; pass < maxPasses; pass++) {
  for (const block of candidates) {
    if (shouldMoveNodeToNextPage(block) && markBreakBefore(block)) {
      // Зелене фарбування в shouldMoveNodeToNextPage()
    }
  }
}
```

#### **Етап 2: Boundary Pass (Всі сторінки)**
```javascript
for (let page = 1; page < pagesNow; page++) {
  const candidate = findBoundarySplitCandidate(candidates, boundaryY, pageTop, pageBottom);
  if (candidate && markBreakBefore(candidate)) {
    // Синє/помаранчеве фарбування в findBoundarySplitCandidate()
  }
}
```

### 📊 **Логування для кожної сторінки:**

```
[PDF] debug:boundary-analysis-page-2 {
  page: 2,
  pageTop: 1123,
  pageBottom: 2246,
  boundaryY: 2186,
  candidatesCount: 15
}

[PDF] debug:BOUNDARY_WILL_MOVE {
  tagName: "DIV",
  className: "exp-item pdf-flow-break",
  textContent: "Senior UX/UI Designer...",
  pageTop: 1123,
  pageBottom: 2246,
  canMoveWhole: true
}

[PDF] debug:boundary-break-created-2 {
  page: 2,
  tagName: "DIV",
  textContent: "Senior UX/UI Designer..."
}
```

### 🎯 **Що тепер видно:**

1. **Перша сторінка:** зелені/червоні блоки + зелені межі
2. **Друга сторінка:** сині/помаранчеві блоки + жовті межі  
3. **Третя сторінка:** сині/помаранчеві блоки + жовті межі
4. **Всі сторінки:** сині межі сторінок

### ✅ **Переваги:**

- **Повна візуалізація** для всіх сторінок
- **Різні кольори** для різних етапів обробки
- **Детальне логування** для кожної сторінки
- **Легка діагностика** проблем з переносами

### 🧪 **Тестування:**

1. Згенеруйте PDF з довгим CV
2. Подивіться на кольорові блоки
3. Перевірте логи для кожної сторінки
4. Переконайтеся, що всі сторінки зафарбовуються фоном

**Тепер ви бачите повну картину перенесення на всіх сторінках!** 🎉
